import Header from "../components/header";

export default function About() {
  return (
    
    <section id="aboutpage" className="site-page">
        
        <Header className="site-header" text="A propos" />

      </section>

  );
}